---
title: "练习方法"
type: docs
---
## 练习方法

本章提供六组由浅入深的练习，覆盖故障检测、Leader选举、脑裂预防、数据恢复、灾备架构和综合实战六个维度。每组练习都包含理论巩固、动手实操和自检标准，确保读者不仅理解原理，还能在真实环境中落地。

> **时间建议**：全部练习约需 8-10 小时。如果时间有限，优先完成练习一（基础理解）、练习三（etcd动手实操）和练习五（综合实战），这三个练习覆盖了本章最核心的知识点。

---

### 练习一：故障检测算法理解与实现（预计 60 分钟）

**目标**：掌握四种故障检测算法的原理差异，能用代码实现基础心跳检测和 Phi Accrual 检测器，并通过实验对比两者的检测精度。

#### 第一步：理论梳理（15 分钟）

用自己的话回答以下问题，不要翻书，测试自己的理解深度：

1. 心跳检测的两个核心参数是什么？超时时间为什么通常设为心跳间隔的 2-3 倍？
2. Phi Accrual 检测器为什么比固定超时检测器更适合网络环境复杂的场景？Phi=1 和 Phi=8 分别代表什么含义？
3. Gossip 协议和 SWIM 协议的核心区别是什么？SWIM 解决了 Gossip 的哪两个问题？
4. 在以下场景中，你会选择哪种检测算法？简述理由：
   - 5 节点 etcd 集群（单机房）
   - 100+ 节点微服务集群（跨机房）
   - 对延迟极度敏感的金融交易系统

#### 第二步：实现心跳检测器（20 分钟）

使用 Python 实现一个完整的心跳检测器，要求：

```python
"""
练习要求：
1. 实现 HeartbeatDetector 类
2. 支持节点注册、心跳接收、状态检查
3. 状态从 ALIVE → SUSPECTED → DEAD 逐步升级
4. 支持回调通知状态变更
"""

import threading
import time
from enum import Enum

class NodeState(Enum):
    ALIVE = "alive"
    SUSPECTED = "suspected"
    DEAD = "dead"

class HeartbeatDetector:
    def __init__(self, heartbeat_interval=1.0, timeout=3.0):
        """
        Args:
            heartbeat_interval: 心跳间隔（秒）
            timeout: 超时判定时间（秒），建议设为 heartbeat_interval 的 2-3 倍
        """
        self.heartbeat_interval = heartbeat_interval
        self.timeout = timeout
        self.nodes = {}           # node_id -> last_heartbeat_time
        self.node_states = {}     # node_id -> NodeState
        self.state_log = []       # 记录所有状态变更，用于分析
        self.callbacks = []
        self._lock = threading.Lock()

    def register_node(self, node_id):
        """注册新节点，初始状态为 ALIVE"""
        # 请实现
        pass

    def on_heartbeat(self, node_id):
        """收到心跳时调用，更新时间戳并恢复状态"""
        # 请实现：如果节点从 SUSPECTED/DEAD 恢复，触发回调
        pass

    def check_all(self):
        """定期检查所有节点状态"""
        # 请实现：根据超时时间判断状态
        # SUSPECTED: elapsed > heartbeat_interval * 1.5
        # DEAD: elapsed > timeout
        pass

    def on_state_change(self, callback):
        """注册状态变更回调：callback(node_id, old_state, new_state)"""
        self.callbacks.append(callback)

    def _notify(self, node_id, old_state, new_state):
        """通知所有回调"""
        self.state_log.append((time.time(), node_id, old_state, new_state))
        for callback in self.callbacks:
            callback(node_id, old_state, new_state)
```

#### 第三步：实验对比（25 分钟）

编写测试脚本，模拟以下三种网络环境并对比检测效果：

```python
"""
实验设计：对比固定超时 vs Phi Accrual 在不同网络条件下的检测精度

场景 A（稳定网络）：心跳间隔 1s，延迟稳定在 10-20ms
场景 B（抖动网络）：心跳间隔 1s，延迟在 10-500ms 间随机波动
场景 C（网络分区）：前 30s 正常，之后完全无法收到心跳

对每个场景，分别用固定超时检测器和 Phi Accrual 检测器检测一个故障节点，
记录：
- 故障检测延迟（从节点实际故障到检测器判定 DEAD）
- 误判次数（在节点正常时误判为 SUSPECTED/DEAD）
- 漏判次数（在节点故障后仍未判定为 DEAD）

输出对比表格，分析两种检测器的优劣。
"""
```

**自检标准**：

- [ ] 能解释心跳间隔与超时时间的参数关系及调优方法
- [ ] 能手写心跳检测器的核心逻辑（不看参考代码）
- [ ] 能说出 Phi Accrual 在抖动网络场景下的优势及原因
- [ ] 三种网络场景的对比实验已完成，结论清晰
- [ ] 能列举至少两个使用 Phi Accrual 的开源系统

---

### 练习二：Raft Leader 选举模拟（预计 75 分钟）

**目标**：深入理解 Raft 选举的完整流程——任期递增、随机超时、投票规则、日志一致性校验，并能模拟选举过程中的各种边界场景。

#### 第一步：理解选举核心流程（15 分钟）

在白纸或 Mermaid 中画出以下流程：

1. Follower 超时未收到心跳 → 自增 Term → 转为 Candidate → 发起 RequestVote
2. Candidate 收到多数票 → 成为 Leader → 开始发送心跳
3. Candidate 发现更高 Term 的消息 → 回退为 Follower
4. 两个 Candidate 同时发起选举 → 票数分散 → 超时后重新选举（随机化打破活锁）

画完后检查：流程中是否覆盖了所有状态转换路径？

#### 第二步：实现选举模拟器（40 分钟）

```python
"""
练习要求：
1. 实现一个 5 节点的 Raft 选举模拟器
2. 每个节点有完整的状态机：FOLLOWER / CANDIDATE / LEADER
3. 模拟网络分区场景
4. 观察选举过程中的 Term 递增和投票竞争

关键测试场景：
- 正常选举：Leader 宕机后多久选出新 Leader？
- 同时选举：两个 Candidate 同时发起投票，结果如何？
- 网络分区：{A,B} 和 {C,D,E} 分区后各发生什么？
- 日志落后：Candidate 的日志比 Follower 落后时会发生什么？
"""

import random
import threading
import time

class LogEntry:
    def __init__(self, term, command):
        self.term = term
        self.command = command

class RaftNode:
    FOLLOWER = "follower"
    CANDIDATE = "candidate"
    LEADER = "leader"

    def __init__(self, node_id, peers):
        self.node_id = node_id
        self.peers = peers          # 其他节点的引用
        self.state = self.FOLLOWER
        self.current_term = 0
        self.voted_for = None
        self.log = []               # 日志条目列表
        self.election_timeout = self._random_timeout()
        self.last_heartbeat = time.time()
        self.votes_received = 0
        self._lock = threading.Lock()
        self.event_log = []         # 记录所有事件，用于分析

    def _random_timeout(self):
        """随机化选举超时，避免活锁。Raft 论文建议 [150ms, 300ms]"""
        return random.uniform(0.15, 0.3)

    def start_election(self):
        """
        发起选举：
        1. 自增 Term
        2. 转为 Candidate
        3. 给自己投一票
        4. 向所有 Peer 发送 RequestVote RPC
        """
        # 请实现
        pass

    def handle_vote_request(self, term, candidate_id,
                           last_log_index, last_log_term):
        """
        处理投票请求：
        1. 如果 term < current_term → 拒绝，返回当前 Term
        2. 如果 term > current_term → 更新 Term，重置 voted_for
        3. 检查日志是否至少和候选人一样新
        4. 如果还没给其他人投票 → 投票给候选人
        """
        # 请实现
        pass

    def handle_vote_response(self, term, vote_granted):
        """处理投票响应，计票并判断是否成为 Leader"""
        # 请实现
        pass

    def check_election_timeout(self):
        """检查选举超时，如果超时则发起选举"""
        # 请实现
        pass

    def step_down(self, new_term):
        """回退为 Follower"""
        # 请实现
        pass
```

#### 第三步：场景测试与分析（20 分钟）

运行以下测试场景并记录观察结果：

| 测试场景 | 操作步骤 | 预期观察 |
|---------|---------|---------|
| 正常选举 | 启动 5 节点，等待选出 Leader，杀掉 Leader | 新 Leader 在 150-300ms 内选出 |
| 票数分裂 | 两个 Candidate 同时发起投票 | 无人获得多数票，随机超时后重选 |
| 网络分区 | 将 5 节点分为 {A,B} 和 {C,D,E} | {C,D,E} 选出新 Leader，{A,B} 无法选举 |
| 日志一致性 | 让 Candidate C 的日志落后于 Follower D | D 拒绝投票给 C，C 无法当选 |
| Term 竞争 | 让一个高 Term 的 Candidate 加入 | 所有节点回退为 Follower |

**自检标准**：

- [ ] 能画出 Raft 选举的完整状态机（FOLLOWER ↔ CANDIDATE → LEADER）
- [ ] 能解释"随机超时"如何解决活锁问题
- [ ] 能解释"日志最新优先"投票规则如何保证数据一致性
- [ ] 网络分区场景下，多数分区和少数分区的行为差异清晰
- [ ] 能说明为什么集群节点数建议为奇数

---

### 练习三：用 Docker 搭建 etcd 集群并模拟故障转移（预计 90 分钟）

**目标**：在真实环境中搭建 3 节点 etcd 集群，手动触发 Leader 故障，观察自动选举过程，理解 Raft 协议在生产系统中的实际表现。

#### 第一步：环境搭建（20 分钟）

```bash
# 1. 确保 Docker 已安装
docker --version

# 2. 拉取 etcd 镜像
docker pull quay.io/coreos/etcd:v3.5.9

# 3. 使用 Docker Compose 快速搭建 3 节点集群
cat > docker-compose.yml << 'EOF'
version: '3.8'
services:
  etcd1:
    image: quay.io/coreos/etcd:v3.5.9
    container_name: etcd1
    command:
      - etcd
      - --name=etcd1
      - --listen-client-urls=http://0.0.0.0:2379
      - --advertise-client-urls=http://etcd1:2379
      - --listen-peer-urls=http://0.0.0.0:2380
      - --initial-advertise-peer-urls=http://etcd1:2380
      - --initial-cluster=etcd1=http://etcd1:2380,etcd2=http://etcd2:2380,etcd3=http://etcd3:2380
      - --initial-cluster-state=new
    ports:
      - "2379:2379"

  etcd2:
    image: quay.io/coreos/etcd:v3.5.9
    container_name: etcd2
    command:
      - etcd
      - --name=etcd2
      - --listen-client-urls=http://0.0.0.0:2379
      - --advertise-client-urls=http://etcd2:2379
      - --listen-peer-urls=http://0.0.0.0:2380
      - --initial-advertise-peer-urls=http://etcd2:2380
      - --initial-cluster=etcd1=http://etcd1:2380,etcd2=http://etcd2:2380,etcd3=http://etcd3:2380
      - --initial-cluster-state=new

  etcd3:
    image: quay.io/coreos/etcd:v3.5.9
    container_name: etcd3
    command:
      - etcd
      - --name=etcd3
      - --listen-client-urls=http://0.0.0.0:2379
      - --advertise-client-urls=http://etcd3:2379
      - --listen-peer-urls=http://0.0.0.0:2380
      - --initial-advertise-peer-urls=http://etcd3:2380
      - --initial-cluster=etcd1=http://etcd1:2380,etcd2=http://etcd2:2380,etcd3=http://etcd3:2380
      - --initial-cluster-state=new
EOF

# 4. 启动集群
docker-compose up -d

# 5. 验证集群健康状态
docker exec etcd1 etcdctl endpoint health --cluster
docker exec etcd1 etcdctl member list -w table
```

#### 第二步：观察正常选举状态（15 分钟）

```bash
# 查看当前 Leader
docker exec etcd1 etcdctl endpoint status --cluster -w table

# 记录当前 Leader 的节点名（Leader 会标注为 "is leader = true"）

# 写入测试数据
docker exec etcd1 etcdctl put /test/key1 "value1"
docker exec etcd1 etcdctl put /test/key2 "value2"

# 验证所有节点数据一致
docker exec etcd2 etcdctl get /test/key1
docker exec etcd3 etcdctl get /test/key1
```

#### 第三步：模拟故障转移（30 分钟）

```bash
# ===== 场景 A：杀掉 Leader 节点 =====
# 先确认当前 Leader
docker exec etcd1 etcdctl endpoint status --cluster -w table

# 假设 Leader 是 etcd2，杀掉它
docker stop etcd2

# 等待 2-3 秒，观察新 Leader 选举
sleep 3
docker exec etcd1 etcdctl endpoint status --cluster -w table

# 验证集群仍可用——新 Leader 应该在剩余节点中产生
docker exec etcd1 etcdctl put /test/key3 "value3"
docker exec etcd3 etcdctl get /test/key3

# ===== 场景 B：恢复旧节点 =====
docker start etcd2
sleep 5
docker exec etcd1 etcdctl endpoint status --cluster -w table
# 观察：旧 Leader 恢复后是 Follower 还是重新当选？

# 验证数据同步——旧节点是否收到了故障期间写入的 key3？
docker exec etcd2 etcdctl get /test/key3

# ===== 场景 C：连续故障 =====
docker stop etcd2
docker stop etcd3
# 集群只剩 1 个节点，无法满足 Quorum
# 尝试写入，观察是否被拒绝
docker exec etcd1 etcdctl put /test/key4 "value4" 2>&amp;1
# 预期：写入失败，因为失去了 Quorum（需要至少 2/3 的节点）
```

#### 第四步：深度观察日志（25 分钟）

```bash
# 查看各节点的 Raft 日志，理解选举过程
docker logs etcd1 2>&amp;1 | grep -i "leader" | tail -20
docker logs etcd3 2>&amp;1 | grep -i "leader" | tail -20

# 观察 Term 递增
docker logs etcd1 2>&amp;1 | grep -i "term" | tail -20

# 记录完整的事件时间线：
# - 故障发生时间
# - 检测到故障的时间
# - 新 Leader 选出的时间
# - 计算实际 RTO（恢复时间）
```

**自检标准**：

- [ ] 3 节点集群搭建成功，所有节点健康
- [ ] 成功观察到 Leader 故障后的自动选举过程
- [ ] 能说明 Quorum 机制在 2 节点故障时的保护作用
- [ ] 能从日志中提取 Term 递增的证据
- [ ] 旧节点恢复后数据同步成功
- [ ] 记录了实际 RTO（通常在 1-5 秒之间）

---

### 练习四：WAL 重放与备份恢复实战（预计 75 分钟）

**目标**：通过 PostgreSQL 的 WAL 机制理解崩溃恢复原理，并练习基于时间点的恢复（PITR）操作。

> **为什么选 PostgreSQL？** 它的 WAL 机制实现清晰、文档完善，是理解预写日志的最佳教材。MySQL 的 Binlog 原理类似，但 PostgreSQL 的 WAL 与数据文件的耦合更紧密，更适合理解"崩溃恢复"这一核心概念。

#### 第一步：搭建 PostgreSQL 并理解 WAL（20 分钟）

```bash
# 1. 启动 PostgreSQL（使用 Docker 最简单）
docker run -d --name pg-practice \
  -e POSTGRES_PASSWORD=practice123 \
  -p 5432:5432 \
  postgres:16

# 2. 进入容器
docker exec -it pg-practice psql -U postgres

# 3. 在 psql 中执行以下命令，观察 WAL 配置
SHOW wal_level;              -- 应为 replica 或 logical
SHOW max_wal_size;           -- WAL 文件大小上限
SHOW checkpoint_timeout;     -- checkpoint 间隔
SHOW wal_buffers;            -- WAL 缓冲区大小
SHOW fsync;                  -- 是否强制刷盘

# 4. 创建测试数据库和表
CREATE DATABASE test_recovery;
\c test_recovery

CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    amount DECIMAL(10,2),
    status VARCHAR(20),
    created_at TIMESTAMP DEFAULT NOW()
);

-- 插入初始数据
INSERT INTO orders (amount, status) VALUES
    (100.00, 'completed'),
    (200.50, 'completed'),
    (50.75, 'pending');

-- 记录当前时间点，后续恢复时使用
SELECT NOW() AS checkpoint_before;
-- 假设输出为 2026-06-26 10:00:00
```

#### 第二步：模拟崩溃场景（20 分钟）

```bash
# 1. 继续插入数据（这些数据在"崩溃"后会丢失）
INSERT INTO orders (amount, status) VALUES
    (300.00, 'completed'),
    (400.25, 'pending');

-- 确认数据已插入
SELECT COUNT(*) FROM orders;  -- 应为 5

# 2. 模拟崩溃：直接停止 PostgreSQL（不执行 SHUTDOWN）
docker stop pg-practice

# 3. 重启 PostgreSQL——这就是"崩溃恢复"
docker start pg-practice

# 4. 验证数据是否恢复
docker exec pg-practice psql -U postgres -d test_recovery \
  -c "SELECT COUNT(*) FROM orders;"
-- WAL 重放会恢复所有已提交的事务，COUNT 应为 5
```

#### 第三步：练习 PITR 基于时间点恢复（35 分钟）

```bash
# 1. 前提：配置 WAL 归档
docker exec pg-practice bash -c "
  mkdir -p /var/lib/postgresql/wal_archive
"

# 修改 postgresql.conf 启用归档
docker exec pg-practice bash -c "
  echo \"archive_mode = on\" >> /var/lib/postgresql/data/postgresql.conf
  echo \"archive_command = 'cp %p /var/lib/postgresql/wal_archive/%f'\" >> /var/lib/postgresql/data/postgresql.conf
"

# 重启使配置生效
docker restart pg-practice
sleep 3

# 2. 记录当前数据状态
docker exec pg-practice psql -U postgres -d test_recovery -c "
  SELECT * FROM orders;
"
# 记录当前行数（假设为 5 行）

# 3. 继续写入数据
docker exec pg-practice psql -U postgres -d test_recovery -c "
  INSERT INTO orders (amount, status) VALUES
      (500.00, 'completed'),
      (600.00, 'completed'),
      (700.00, 'cancelled');
"

# 再次记录时间点
docker exec pg-practice psql -U postgres -d test_recovery -c "
  SELECT NOW() AS after_more_inserts;
"
-- 假设输出为 2026-06-26 10:05:00

# 4. 执行一次手动 checkpoint，确保 WAL 数据落盘
docker exec pg-practice psql -U postgres -d test_recovery -c "CHECKPOINT;"

# 5. 继续插入更多"不应该出现在恢复结果中"的数据
docker exec pg-practice psql -U postgres -d test_recovery -c "
  INSERT INTO orders (amount, status) VALUES
      (800.00, 'completed'),
      (900.00, 'pending');
"

-- 此时总行数应为 10

# 6. 恢复到 10:05:00（即第 8 行插入之前），期望结果为 8 行
# 实际操作需要使用 pg_basebackup + WAL 归档 + recovery.conf
# 以下为恢复流程：

# 停止当前实例
docker stop pg-practice

# 以恢复模式启动
docker run -d --name pg-restore \
  -e POSTGRES_PASSWORD=practice123 \
  -v pgdata_restored:/var/lib/postgresql/data \
  postgres:16

sleep 3

# 配置恢复参数
docker exec pg-restore bash -c "
cat >> /var/lib/postgresql/data/postgresql.conf << 'CONF'
restore_command = 'cp /var/lib/postgresql/wal_archive/%f %p'
recovery_target_time = '2026-06-26 10:05:00'
recovery_target_action = 'pause'
CONF
"

# 提示：实际 PITR 操作比这里展示的更复杂，需要：
# 1. 从基础备份恢复数据目录
# 2. 配置恢复参数
# 3. 启动 PostgreSQL 进入恢复模式
# 4. 恢复完成后切换到读写模式

# 7. 清理练习环境
docker rm -f pg-practice pg-restore
```

**自检标准**：

- [ ] 能解释 WAL 的工作原理：为什么"先写日志再写数据"能保证崩溃恢复？
- [ ] 能说明 WAL 文件的写入流程：内存 → WAL Buffer → WAL File → Data File
- [ ] 能说出 checkpoint 的作用：将内存中的脏页刷到数据文件，可以清理旧 WAL
- [ ] 能描述 PITR 的三步流程：基础备份 → WAL 归档 → 恢复到目标时间点
- [ ] 理解 RTO 与 WAL 重放时间的关系：WAL 越多，恢复越慢

---

### 练习五：灾备架构设计与 RPO/RTO 计算（预计 60 分钟）

**目标**：能够根据业务需求选择合适的灾备等级（Tier 1-6），计算 RPO/RTO 指标，并设计出可落地的备份策略方案。

#### 第一步：RPO/RTO 计算练习（15 分钟）

根据以下业务场景，计算 RPO 和 RTO 的目标值，并选择对应的灾备等级：

| 业务场景 | 业务描述 | 可接受数据丢失 | 可接受服务中断 | 你的选择 |
|---------|---------|--------------|--------------|---------|
| 电商核心交易 | 订单、支付数据 | 0（零丢失） | < 5 分钟 | Tier __ |
| 用户评论系统 | 用户 UGC 内容 | < 1 小时 | < 1 小时 | Tier __ |
| 内部日志分析 | 运维日志收集 | < 24 小时 | < 8 小时 | Tier __ |
| 实时推荐系统 | 用户行为数据 | < 5 分钟 | < 1 分钟 | Tier __ |
| 金融清算系统 | 交易清算记录 | 0（零丢失） | < 30 秒 | Tier __ |

参考答案（先独立思考再对照）：

- **电商核心交易**：Tier 4-5（热备/双活）。RPO=0 需要同步复制，RTO<5min 需要自动切换。
- **用户评论系统**：Tier 2-3（热备站点/温备站点）。UGC 内容丢失 1 小时可接受，恢复时间 1 小时内。
- **内部日志分析**：Tier 1-2（数据备份/热备份站点）。日志数据允许 24h 丢失。
- **实时推荐系统**：Tier 4-5（热备/双活）。RTO<1min 需要自动故障转移，RPO<5min 需要近实时复制。
- **金融清算系统**：Tier 5-6（双活/多活）。零丢失 + 30 秒恢复，这是最高等级要求。

#### 第二步：备份策略设计（25 分钟）

为以下场景设计完整的备份策略：

**场景**：一个 500GB 的电商数据库，业务要求 RPO < 5 分钟，RTO < 30 分钟。

请回答以下问题：

**Q1：备份类型组合**

你的方案：
- 全量备份频率：______________
- 增量备份频率：______________
- WAL/Binlog 保留策略：______________

理由：______________

参考方案：
- 全量备份：每周日凌晨 2 点（业务最低峰）
- 增量备份：每天凌晨 3 点
- WAL 归档：实时归档，保留至少 30 分钟（满足 RPO < 5 分钟）
- 异地复制：同步复制到备用站点

**Q2：恢复时间估算**

恢复步骤及时间估算：
1. 拉取最新全量备份：____________ 分钟
2. 恢复最近的增量备份：____________ 分钟
3. WAL 重放：____________ 分钟
4. 数据库启动和验证：____________ 分钟
总恢复时间：____________ 分钟

是否满足 RTO < 30 分钟的要求？____________

参考估算（基于 SSD 存储、万兆网络）：
1. 拉取全量备份（压缩传输 500GB→约 150GB）：5-8 分钟
2. 恢复增量备份：1-2 分钟
3. WAL 重放（30 分钟的 WAL）：3-5 分钟
4. 启动和验证：2-3 分钟
5. **总计：11-18 分钟**，满足 RTO < 30 分钟

**Q3：备份验证策略**

验证频率和方法：
- 自动验证：______________
- 手动恢复演练：______________
- 验证指标：______________

参考方案：
- 自动验证：每次备份完成后自动执行 checksum 校验 + 恢复到测试实例验证可读性
- 手动恢复演练：每月一次，完整执行恢复流程并记录实际 RTO
- 验证指标：备份完整性（checksum 匹配）、恢复时间（< 目标 RTO）、数据一致性（行数/crc32 比对）

#### 第三步：灾难场景推演（20 分钟）

推演以下灾难场景的完整处理流程：

**场景**：主数据中心（上海）因电力故障整体不可用，备用数据中心（杭州）需要接管服务。

请写出完整的处理步骤和时间线：

T+0s    ：___________________（故障发生）
T+30s   ：___________________（故障检测）
T+??s   ：___________________（故障确认）
T+??s   ：___________________（Fencing 执行）
T+??s   ：___________________（服务切换）
T+??s   ：___________________（数据验证）
T+??s   ：___________________（服务恢复）

总 RTO：____ 分钟
是否存在数据丢失？RPO：____ 

**自检标准**：

- [ ] 能为 5 种不同业务场景选择合适的灾备等级
- [ ] 能设计出包含全量+增量+WAL 的组合备份策略
- [ ] 能估算恢复时间并判断是否满足 RTO 要求
- [ ] 能完整推演数据中心级灾难的处理时间线
- [ ] 理解 RPO=0 与同步复制的关系及性能代价

---

### 练习六：综合实战——设计高可用架构方案（预计 120 分钟）

**目标**：综合运用本章所有知识，为一个具体业务系统设计完整的高可用方案，包括故障检测、自动转移、数据恢复和灾备架构四个层面。

#### 第一步：场景分析（20 分钟）

**业务背景**：你负责设计一个在线支付系统的高可用架构，具体要求如下：

| 维度 | 要求 |
|-----|------|
| 业务类型 | 支付交易、账户余额管理 |
| 数据量 | 100TB 交易记录，日增 50GB |
| 并发量 | 峰值 10,000 TPS |
| 可用性目标 | 99.99%（年停机 < 53 分钟） |
| 数据安全 | RPO = 0（零数据丢失） |
| 恢复时间 | RTO < 30 秒 |
| 部署环境 | 跨 2 个可用区（上海-AZ1、上海-AZ2） |
| 预算约束 | 中等（非无限预算） |

#### 第二步：分层设计（60 分钟）

请按以下四个层面分别设计：

**层面一：故障检测设计**

选择的检测算法：______________
理由：______________

心跳间隔：____________ ms
超时阈值/Phi 阈值：____________
检测器部署方式：____________

告警策略：
- SUSPECTED 状态：____________
- DEAD 状态：____________

**层面二：故障转移设计**

Leader 选举算法：____________
集群节点数：____________（奇数/偶数？理由？）

脑裂预防方案：
- 主要手段：____________
- 辅助手段：____________

Fencing 方案：
- 硬 Fencing：____________
- 软 Fencing：____________

切换流程（灰度）：
1. ____________
2. ____________
3. ____________
4. ____________

**层面三：数据恢复设计**

数据库引擎：____________
WAL 配置：
- wal_level：____________
- synchronous_commit：____________（为满足 RPO=0）
- wal_keep_size：____________

备份策略：
- 全量备份：____________
- 增量备份：____________
- WAL 归档：____________

恢复流程：
1. ____________
2. ____________
3. ____________
估算恢复时间：____________

**层面四：灾备架构设计**

灾备等级选择：Tier ____
理由：____________

架构拓扑：
- AZ1（主）：____________
- AZ2（备）：____________

数据同步方式：____________（同步/异步/半同步）

故障切换流程：
1. ____________
2. ____________
3. ____________

年度成本估算：____________

#### 第三步：方案评审清单（40 分钟）

对照以下清单逐项审查你的方案：

**可用性审查**

| 检查项 | 是否满足 | 说明 |
|--------|---------|------|
| 单节点故障是否自动恢复？ | | |
| 单可用区故障是否自动切换？ | | |
| 故障检测延迟是否 < 10 秒？ | | |
| 故障转移时间是否 < 30 秒？ | | |
| 数据丢失是否为零（RPO=0）？ | | |
| 年可用性是否达到 99.99%？ | | |

**安全性审查**

| 检查项 | 是否满足 | 说明 |
|--------|---------|------|
| 是否有脑裂防护？ | | |
| 是否有 Fencing 机制？ | | |
| 备份是否异地存储（3-2-1 原则）？ | | |
| 是否有备份恢复验证？ | | |
| 是否有恢复演练计划？ | | |

**成本审查**

| 检查项 | 评估 | 说明 |
|--------|------|------|
| 硬件成本是否合理？ | | |
| 是否有不必要的过度设计？ | | |
| 同步复制的延迟代价是否可接受？ | | |
| 灾备站点的运维成本是否可承受？ | | |

**常见陷阱检查**

| 陷阱 | 是否踩坑 | 纠正方案 |
|------|---------|---------|
| 心跳超时凭经验设定 | | |
| 盲目追求强一致性 | | |
| 只做全量备份 | | |
| 灾备切换一步到位 | | |
| 忽略级联故障 | | |
| 复盘流于形式 | | |

**自检标准**：

- [ ] 四个层面的设计完整且相互协调
- [ ] 故障检测参数有理有据（不是拍脑袋）
- [ ] 故障转移流程包含灰度切换步骤
- [ ] 数据恢复方案能同时满足 RPO=0 和 RTO<30s
- [ ] 灾备方案在成本和可靠性之间取得平衡
- [ ] 至少识别并解决了 3 个潜在风险点

---

### 练习总结与进阶路径

完成以上六组练习后，你应该具备以下能力：

| 能力层级 | 对应练习 | 验证标志 |
|---------|---------|---------|
| **理解** | 练习一 | 能用自己话解释四种检测算法的原理差异 |
| **实现** | 练习二 | 能模拟 Raft 选举并分析各种边界场景 |
| **实操** | 练习三、四 | 能在真实系统中观察故障转移和 WAL 恢复 |
| **设计** | 练习五、六 | 能根据业务需求设计完整的高可用方案 |

#### 进阶练习（供高级读者挑战）

1. **混沌工程实战**：用 Chaos Mesh 对练习三中的 etcd 集群注入 Pod Kill、Network Latency、DNS 故障，观察系统行为，编写故障注入测试用例
2. **多活架构设计**：将练习六的场景升级为跨 3 个城市（上海/北京/深圳）的多活架构，解决数据冲突和路由问题
3. **源码阅读**：阅读 etcd 的 Raft 实现（`etcd/raft/` 目录），对比自己在练习二中的实现，分析工业级实现的差异
4. **性能压测**：对 etcd 集群进行压测（使用 `bench` 工具），测量 Leader 切换期间的请求成功率和延迟变化
